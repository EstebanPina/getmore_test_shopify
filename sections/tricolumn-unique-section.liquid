{% schema %}
  {
    "name": "Tricolumn Section",
    "settings": [
      {
        "type": "text",
        "id": "page_heading",
        "label": "Encabezado de la Página",
        "default": "Bienvenido a Nuestra Tienda"
      },
      {
        "type": "text",
        "id": "section_heading",
        "label": "Encabezado de la Sección",
        "default": "Explora Nuestros Productos"
      },
      {
        "type": "text",
        "id": "section_description",
        "label": "Descripción",
        "default": "Encuentra lo mejor para ti"
      },
      {
        "type": "text",
        "id": "button_label_1",
        "label": "Etiqueta del Botón",
        "default": "Más Información",
        "info": "Texto que aparecerá en el botón."
      },
      {
        "type": "url",
        "id": "button_link_1",
        "label": "Enlace del Botón",
        "default": "https://example.com"
      },
      {
        "type": "checkbox",
        "id": "button_style_secondary_1",
        "label": "Usar Estilo Secundario para el Botón",
        "default": false
      }
    ],
    "presets": [
      {
        "name": "Tricolumn Section",
        "category": "Custom"
      }
    ]
  }
  {% endschema %}
  
  {% stylesheet %}
    .responsive-section {
      padding: 20px;
      background-color: #f9f9f9;
      border: 1px solid #ccc;
    }
  
    .columns-container {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
    }
  
    .column {
      flex: 1 1 100%; /* Por defecto, cada columna toma el 100% del ancho del contenedor */
      text-align: center;
      padding: 10px;
      border: 1px solid #ddd;
    }
  
    /* Media query para pantallas más grandes que 749px */
    @media (min-width: 750px) {
      .column {
        flex: 1 1 33%; /* Cada columna toma aproximadamente un tercio del ancho del contenedor */
      }
    }
    {% endstylesheet %}
  
  <div class="responsive-section">
    <h1>{{ section.settings.page_heading }}</h1>
    <h2>{{ section.settings.section_heading }}</h2>
    <p>{{ section.settings.section_description }}</p>
    <div class="columns-container">
      {%- if section.settings.button_label_1 != blank -%}
        <a href="{{ section.settings.button_link_1 }}"
           class="button {% if section.settings.button_style_secondary_1 %}button--secondary{% else %}button--primary{% endif %}"
           role="button">
          {{ section.settings.button_label_1 }}
        </a>
      {%- endif -%}
    </div>
  </div>
  
  